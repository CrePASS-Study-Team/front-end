import React from 'react';
import Header from '../../components/Header';

const Survey = () => {
  return (
    <div>
      <Header />
      Survey
    </div>
  );
};

export default Survey;
